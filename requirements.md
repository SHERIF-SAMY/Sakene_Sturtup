# متطلبات وحزم المشروع (Project Requirements)

في مشاريع Node.js (مثل هذا المشروع المبني باستخدام React و Vite)، يتم إدارة جميع الحزم والمتطلبات من خلال ملف `package.json`.
فيما يلي قائمة بجميع الحزم الأساسية وحزم التطوير التي يحتاجها المشروع للعمل، والتي قمنا بتثبيتها بالفعل باستخدام الأمر `npm install`.

## الحزم الأساسية (Dependencies)
هذه هي الحزم التي يعتمد عليها المشروع بشكل مباشر ليعمل:

- **react** (`^19.2.0`): مكتبة الواجهات الأساسية.
- **react-dom** (`^19.2.0`): للتعامل مع شجرة عناصر المتصفح (DOM).
- **react-router-dom** (`^7.13.1`): لإدارة التنقل (Routing) بين صفحات التطبيق.
- **tailwindcss** (`^4.2.1`): إطار عمل CSS المستخدم لتنسيق واجهة المستخدم بسرعة.
- **@tailwindcss/vite** (`^4.2.1`): إضافة Tailwind للعمل بتوافق مع بيئة Vite.
- **framer-motion** (`^12.35.0`): مكتبة قوية لإضافة الحركات والأنيميشن للواجهات.
- **lucide-react** (`^0.577.0`): مكتبة أيقونات حديثة ومفتوحة المصدر.
- **@supabase/supabase-js** (`^2.99.1`): عميل الاتصال بقواعد بيانات وخدمات Supabase (البديل مفتوح المصدر لـ Firebase).

## حزم التطوير (Dev Dependencies)
هذه الحزم تُستخدم فقط في بيئة التطوير للمساعدة في بناء المشروع، فحصه، وتشغيله محلياً ولا يتم تضمينها في النسخة النهائية للمستخدم:

- **vite** (`^7.3.1`): أداة بناء وتشغيل المشروع (أسرع بكثير من Webpack).
- **typescript** (`~5.9.3`): لغة TypeScript لضمان كتابة كود خالي من أخطاء الأنواع.
- **eslint** (`^9.39.1`) وملحقاته: لفحص الكود والتأكد من جودته وتوحيد أسلوب الكتابة.
- **@vitejs/plugin-react** (`^5.1.1`): دعم React داخل بيئة Vite.
- **@types/...**: حزم تعريفات الأنواع لـ React و Node (مثل `@types/react` و `@types/node`).

---

**ملاحظة هامة:**
لست بحاجة لتثبيت هذه الحزم واحدة تلو الأخرى يدوياً. يكفيك دائماً فتح سطر الأوامر (Terminal) في مسار المشروع وكتابة الأمر التالي:
```bash
npm install
```
وسيقوم Node.js تلقائياً بقراءة ملف `package.json` وتنزيل كافة هذه الحزم إلى مجلد `node_modules`.
